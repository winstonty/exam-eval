# Exam-Eval
Program that prepares exams in PDF format and evaluates models on them.

## Set up
Run ```/bin/sh setup.sh``` to install python dependencies/ollama, and run ```python3 pdf-preparer``` to extract data into a json format.

Run ```python3 evaluate.py``` to run it against your selected models.